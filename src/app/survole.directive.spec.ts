import { SurvoleDirective } from './survole.directive';

describe('SurvoleDirective', () => {
  it('should create an instance', () => {
    const directive = new SurvoleDirective();
    expect(directive).toBeTruthy();
  });
});
